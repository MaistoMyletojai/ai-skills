// AUTOGENERATED by qa-runner agent for ticket #{{TICKET}} — DO NOT COMMIT.
// Path convention:
//   Dserve.SelfService.Integration/QaScratch/T{{TICKET}}/AC{{N}}_{{SHORT_NAME}}Tests.cs
//
// Template variables (replace before writing):
//   {{TICKET}}      — ticket number (e.g. 4621)
//   {{N}}           — AC index, 1-based (e.g. 1, 2, …)
//   {{SHORT_NAME}}  — short PascalCase name for the test class
//                     (e.g. "OrderListReturnsOrders")
//   {{AC_TEXT}}     — the verbatim AC text, escaped for C# string literal
//   {{AREA}}        — contracts namespace area (e.g. "Orders", "Tablets")
//   {{TEST_BODY}}   — generated arrange/act/assert block
//
// The DisplayName MUST start with "AC{{N}}: " so the orchestrator can
// map test results back to the AC id.

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Threading.Tasks;
using Dserve.Contracts.{{AREA}};
using Dserve.Core.SelfServiceApiRoutes;
using FluentAssertions;
using Xunit;
using static Dserve.Core.SelfServiceApiRoutes.SelfServiceApiRoutes;

namespace Dserve.SelfService.Integration.QaScratch.T{{TICKET}}
{
    public class AC{{N}}_{{SHORT_NAME}}Tests : IntegrationTest
    {
        [Fact(DisplayName = "AC{{N}}: {{AC_TEXT}}")]
        public async Task Verify_AC{{N}}()
        {
{{TEST_BODY}}
        }
    }
}
